<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProteusMMX.Views.PurchaseOrder.Templates.ViewCells.PurchaseorderViewCell">
    <ViewCell.View>
        <Frame BorderColor="Gray">
            <StackLayout >
                <Grid RowSpacing="5">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                <Label x:Name="PurchaseOrderNumber" />
                <Label Text=":" Grid.Column="1"  />
                <Label Text="{Binding PurchaseOrderNumber}" Grid.Column="2" />

            </Grid>
            <!--<BoxView Color="Gray"  VerticalOptions="Start" HorizontalOptions="FillAndExpand" HeightRequest="1" />-->
        </Grid>
            </StackLayout>
        </Frame>
    </ViewCell.View>
</ViewCell>