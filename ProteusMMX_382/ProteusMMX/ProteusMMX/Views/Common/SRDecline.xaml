<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                  xmlns:border="clr-namespace:Syncfusion.XForms.Border;assembly=Syncfusion.Core.XForms"
                 xmlns:dependencyinterface="clr-namespace:ProteusMMX.DependencyInterface"
                 xmlns:Control1="clr-namespace:ProteusMMX.Controls"
                 x:Class="ProteusMMX.Views.Common.SRDecline">

    <ContentPage.Resources>
        <ResourceDictionary>
            <!--<converter:BooleanToColorConverter x:Key="ColorConverter" />-->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout  Margin="40,40,40,40" Padding="20,20,20,20" Spacing="10" VerticalOptions="Center"  BackgroundColor="White"  >

        <Label Grid.Row="0" x:Name="HeaderLabel" FontSize="Medium"  HorizontalOptions="Center" Text="Notes" Style="{StaticResource BoldLabelStyle}" Grid.ColumnSpan="2" />

        <!--<Label Grid.Row="1" x:Name="Username" Style="{StaticResource BoldLabelStyle}" Grid.ColumnSpan="2" />
            <Entry Grid.Row="2" x:Name="UserLabel"  TextColor="{StaticResource ColorLabel}" Grid.ColumnSpan="2" />

            <Label Grid.Row="3" x:Name="Pass" Style="{StaticResource BoldLabelStyle}" Grid.ColumnSpan="2" />-->

        <border:SfBorder CornerRadius="5">
            <Control1:UnderLineEditor Grid.Row="1" x:Name="Reason" TextColor="{StaticResource ColorLabel}"  Grid.ColumnSpan="2"/>
        </border:SfBorder>
        <Button  x:Name="ok" Clicked="OK_Clicked" Text="OK" HeightRequest="30" Grid.Row="2" Grid.Column="0" CornerRadius="5"/>
        <Button x:Name="cancel" Clicked= "CANCEL_Clicked" Text="Cancel" HeightRequest="30" Grid.Row="2" Grid.Column="1" CornerRadius="5"/>

    </StackLayout>

</pages:PopupPage>